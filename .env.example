# ===========================================
# 财报播客生成器 - 环境变量配置示例
# ===========================================
# 请复制此文件为 .env 并填入真实的API密钥和配置信息

# ===========================================
# 数据库配置 (Neon PostgreSQL)
# ===========================================
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
# 示例: postgresql://user:pass@ep-cool-darkness-123456.us-east-1.aws.neon.tech/neondb?sslmode=require

# ===========================================
# Finnhub API 配置 (获取财报文字稿)
# ===========================================
FINNHUB_API_KEY=your_finnhub_api_key_here
# 获取地址: https://finnhub.io/dashboard
# 免费版限制: 60 calls/minute

# ===========================================
# Google Gemini API 配置 (AI摘要生成)
# ===========================================
GEMINI_API_KEY=your_gemini_api_key_here
# 获取地址: https://aistudio.google.com/app/apikey
# 模型: gemini-1.5-flash 或 gemini-1.5-pro
GEMINI_MODEL=gemini-1.5-flash

# ===========================================
# Coze TTS API 配置 (语音合成)
# ===========================================
COZE_API_KEY=your_coze_api_key_here
COZE_BOT_ID=your_coze_bot_id_here
# 获取地址: https://www.coze.com/
# 或使用火山引擎TTS API

# ===========================================
# 云存储配置 (音频文件存储)
# ===========================================
# 选择一种云存储服务配置

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# AWS S3 (备选)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-podcast-bucket

# Cloudflare R2 (备选)
# R2_ACCOUNT_ID=your_r2_account_id
# R2_ACCESS_KEY_ID=your_r2_access_key
# R2_SECRET_ACCESS_KEY=your_r2_secret_key
# R2_BUCKET_NAME=your-r2-bucket

# ===========================================
# 应用配置
# ===========================================
# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 日志文件路径
LOG_FILE_PATH=./logs/app.log

# 音频文件本地存储路径
AUDIO_OUTPUT_PATH=./output/audio

# 摘要文件本地存储路径
SUMMARY_OUTPUT_PATH=./output/summaries

# 默认语言 (zh-CN 或 en-US)
DEFAULT_LANGUAGE=zh-CN

# TTS 语音配置
TTS_VOICE_SPEED=1.0
TTS_VOICE_PITCH=0.0
TTS_AUDIO_FORMAT=mp3

# ===========================================
# GitHub Actions 专用配置
# ===========================================
# 这些变量需要在 GitHub Repository Secrets 中配置
# 不要在本地 .env 文件中设置

# GITHUB_TOKEN (自动提供)
# DATABASE_URL (在 Secrets 中设置)
# FINNHUB_API_KEY (在 Secrets 中设置)
# GEMINI_API_KEY (在 Secrets 中设置)
# COZE_API_KEY (在 Secrets 中设置)
# BLOB_READ_WRITE_TOKEN (在 Secrets 中设置)

# ===========================================
# 开发环境配置
# ===========================================
# 开发模式 (development, production)
NODE_ENV=development

# 是否启用详细日志
DEBUG_MODE=true

# API 请求超时时间 (毫秒)
API_TIMEOUT=30000

# 最大重试次数
MAX_RETRIES=3

# ===========================================
# 安全提醒
# ===========================================
# 1. 永远不要将真实的 API 密钥提交到版本控制系统
# 2. 确保 .env 文件已添加到 .gitignore
# 3. 定期轮换 API 密钥
# 4. 使用最小权限原则配置 API 密钥
# 5. 在生产环境中使用环境变量而非 .env 文件